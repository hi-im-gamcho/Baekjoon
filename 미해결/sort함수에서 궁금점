# 다중 정렬
a = [(0,1), (2,3), (1,3), (5,10), (4,8), (9,9), (4,9)]
a.sort(key=lambda a:(a[1],a[0]))
# 두번째 인자로 정렬 후 첫번째 인자로 정렬

# 결과 = [(0,1), (1,3), (2,3), (4,8), (4,9), (9,9), (5,10)]

3번째 줄이 잘 이해가 가지 않습니다.

a[1] 과 a[0]은 각각 (2,3), (0,1) 이라고 생각하는데 

람다를 이용해여 3행처럼 코드를 작성하면 

2번째 인자로 정렬 후 첫번째 인자로 정렬이 되는 것인지 궁금합니다. 